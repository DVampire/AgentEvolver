Create `add.py` in the workspace root: a function `add(a, b)` returning the sum of two
numbers, with a type-annotated signature and a one-line docstring.

Then write `test_add.py` beside it with a few cases, run it, and report the result.
