"""Linear (Composio) — from the Langflow `composio` bundle (ported)."""

from agentevolver.registry import PLUGIN
from agentevolver.plugins.types import ComposioPlugin


@PLUGIN.register_module(force=True)
class ComposioLinearComposioPlugin(ComposioPlugin):
    name: str = "composio.linear_composio"
    display_name: str = 'Linear'
    description: str = 'Execute Linear actions via Composio.'
    kind: str = "tool"
    bundle: str = "composio"
    bundle_label: str = "Composio"
    source: str = "langflow/bundles/composio"
    status: str = "complete"
    app_name: str = "linear"
