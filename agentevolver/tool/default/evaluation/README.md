# Evaluation tools

Benchmark-specific bridges exposed to benchmark agents. Dataset and scoring implementations
belong to `agentevolver.benchmark`; host/container transport is launcher infrastructure.
