# Runtime-generated extension state
manifest.json
# A manifest left root-owned and 0600 by a run inside a root container blocks the
# rebuild, so it gets renamed aside rather than deleted — unreadable here, and a file
# nobody can read is not one to remove on a guess. Removing it needs the owner's sudo.
manifest.json.root-owned-*
.versions/
.journal/
