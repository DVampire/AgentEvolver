# Build context for docker/base/Dockerfile is the repo root (it installs the
# framework). Keep the context lean and reproducible.
.git
output
# Local benchmark data and reference checkouts are mounted with the repo at
# runtime; copying them into the IDE's base image can add many gigabytes.
datasets
others
.pytest_cache
.ruff_cache
frontend/dist
**/__pycache__
*.pyc
.venv
venv
**/node_modules
docker/*/*.log
.env
.claude
.agentevolver
*.egg-info
