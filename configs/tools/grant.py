# The middle of three ways to widen a roster: a named component to a named process that
# is already running. Mount it wherever a run may evolve a capability that a live
# sub-agent needs — the other two paths need no tool at all, since a dispatch carries
# its child's scope and `accepts_evolved` is declared on the agent.
grant_tool = dict(
    enable_evolving = False,
)
